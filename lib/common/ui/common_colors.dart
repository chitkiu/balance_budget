import 'package:flutter/material.dart';

class CommonColors {
  static const expense = Colors.redAccent;
  static const income = Colors.green;
  ///Used as default color, where we need to set some color
  static const defColor = Colors.grey;
}